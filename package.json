{
  "name": "cypress_test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:notebook": "cypress run --config-file cypress-notebook-config.js",
    "test:mobile": "cypress run --config-file cypress-mobile-config.js",
    "test:headless:chrome": "cypress run --browser chrome --headless --config-file cypress-notebook-config.js",
    "test:headless:electron": "cypress run --browser electron --headless --config-file cypress-mobile-config.js",
    "test:headed:chrome": "cypress run --headed --browser chrome --config-file cypress-mobile-config.js",
    "test:headed:electron": "cypress run --headed --browser electron --config-file cypress-notebook-config.js"
},

  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.3.2"
  }
}
